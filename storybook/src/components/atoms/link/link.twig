{# Component: Link (Atom) #}
{% set link_attributes = create_attribute(attributes|default({})) %}
{% set link_classes = ['link'] %}

<a href="{{ url }}" {{ link_attributes.addClass(link_classes) }}>{{ title }}</a>
